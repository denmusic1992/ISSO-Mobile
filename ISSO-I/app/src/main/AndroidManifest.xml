<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.development.aisisso.isso_i"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/name_app"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/name_app"
            android:theme="@style/Advanced">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/title_activity_synchronize"
            android:theme="@android:style/Theme.Holo.Dialog.NoActionBar">
        </activity>
        <activity
            android:name=".NEWSettingsActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/title_activity_synchronize"
            android:theme="@android:style/Theme.Holo.Dialog.NoActionBar">
        </activity>
        <activity
            android:name=".UploadPhotoActivity"
            android:label="@string/title_activity_synchronize"
            android:theme="@android:style/Theme.Holo.Dialog.NoActionBar">
        </activity>
        <activity
            android:name=".UploadSchemeActivity"
            android:label="@string/title_activity_synchronize"
            android:theme="@android:style/Theme.Holo.Dialog.NoActionBar">
        </activity>
        <activity
            android:name=".ViewInfoActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/title_activity_issoView"
            android:theme="@style/Advanced" >
        </activity>
        <activity
            android:name=".AdvancedSettings"
            android:label="@string/title_activity_settings"
            android:theme="@style/Advanced" >
        </activity>
        <activity
            android:name=".ABDM_TableLayout"
            android:label="@string/title_activity_settings"
            android:theme="@style/Advanced" >
        </activity>
        <activity
            android:name=".AdvancedDownloadInfo"
            android:theme="@style/DialogTheme">
        </activity>
        <activity
            android:name=".ABDM_TableLayout_I_DEFECT"
            android:configChanges="orientation|screenSize"
            android:label="@string/title_activity_download"
            android:theme="@style/Advanced" >
        </activity>
        <activity
            android:name=".AdvancedDefectActivity"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Advanced" >
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key"/>

        <activity
            android:name=".MapsActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/title_activity_maps">
        </activity>
        <activity
            android:name=".ViewIssoGeolocation"
            android:theme="@android:style/Theme.Holo.Dialog.NoActionBar">
        </activity>
    </application>

</manifest>
