<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             x:Class="ISSO_I.IssoViewPages.ThumbContentView">
    <ContentView.Content>
        <ListView CachingStrategy="RecycleElement" x:Name="lv_schemes" HasUnevenRows="True" x:FieldModifier="public">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout>
                            <ffimageloading:CachedImage Source="{Binding ImageSource}" Margin="10, 5, 10, 5" Aspect="AspectFill">
                                <ffimageloading:CachedImage.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="Thumbnail_Clicked" />
                                </ffimageloading:CachedImage.GestureRecognizers>
                            </ffimageloading:CachedImage>
                            <Label Text="{Binding Info}" Margin="10, 5, 10, 5" HorizontalTextAlignment="Center" FontSize="Default" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentView.Content>
</ContentView>