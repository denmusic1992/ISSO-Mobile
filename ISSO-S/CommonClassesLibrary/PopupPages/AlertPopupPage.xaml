<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:CommonClassesLibrary.Converters;assembly=CommonClassesLibrary"
             x:Class="CommonClassesLibrary.PopupPages.AlertPopupPage">
    <ContentView.Resources>
        <converters:ButtonStyleConverter x:Key="ButtonStyleConverter" />
    </ContentView.Resources>
    <StackLayout>
        <Label x:Name="InfoLabel" Style="{StaticResource LabelSmall}" Margin="10"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Button x:Name="ButtonCancel" Style="{StaticResource ButtonStandardWhite}" Grid.Column="0"/>
            <Button x:Name="ButtonConfirm" Grid.Column="1" />
        </Grid>
    </StackLayout>
</ContentView>